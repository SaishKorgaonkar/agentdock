# 2026-09-07 — scaffold

**Goal.** Establish the deployable application structure and frontend requested for AgentDock.

**Landed.**

- Created a pnpm/Turborepo workspace with one root lockfile and verification command.
- Scaffolded `apps/web` with Next.js 16 App Router, TypeScript, Tailwind CSS, and an AgentDock landing page.
- Scaffolded separate Fastify orchestrator and paid risk API services with production builds and tested health endpoints.
- Added CI, environment template, root security-focused `.gitignore`, and repository setup documentation.
- Verified `pnpm check`: lint, type checks, two service tests, and three production builds pass.
- Smoke-tested all three local processes: web title rendered and both service health endpoints returned HTTP 200.
- Created public GitHub repository `SaishKorgaonkar/agentdock` and pushed the scaffold to `main`.

**Didn't land.**

- Sponsor feasibility calls remain blocked on testnet accounts/credentials and CRE setup.

**Learned.**

- pnpm 11 requires explicit build-script allowlisting; only `esbuild`, `sharp`, and `unrs-resolver` are allowed in workspace configuration.

**Open.**

- Run Phase 0 Hedera, ENSv2, Chainlink CRE, and staging proofs before implementing product-domain features.
